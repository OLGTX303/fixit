# Copy to .env.production.local before building for Android
# Use your machine's LAN IP so the emulator/device can reach the API:
#   emulator: 10.0.2.2 maps to host localhost
#   physical device: use your computer's IP e.g. 192.168.1.10
VITE_API_URL=http://10.0.2.2:8080/api